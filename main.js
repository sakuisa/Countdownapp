'use strict'

{
 const timer = document.getElementById('timer');
 const btn = document.getElementById('btn');
 
//  終了時刻と求める（開始時に終了時刻を取得したい）
btn.addEventListener('click' , () => {
  const endTime = new Date().getTime() + 3 * 1000;
})
//  残り時間を求める
//  タイマーの終了

}